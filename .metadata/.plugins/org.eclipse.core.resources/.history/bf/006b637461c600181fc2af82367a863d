package pkg.Question3;
import static org.junit.Assert.assertEquals;

import org.junit.*;
import org.junit.rules.ErrorCollector;
import org.junit.rules.Verifier;		
import org.junit.matchers.*;

public class Que3 {
	//ClassThree obj = new ClassThree();
	//JUnitMatchers object=new JUnitMatchers();
	@Rule
	public 	ErrorCollector collector = new ErrorCollector();
	@Test
	public void testFunc() {
		collector.addError(new Throwable("Error on 1st line"));
		collector.addError(new Throwable("There is an error in second line"));
		
		//try {	
			Assert.assertTrue("a "=="b ");
			//int val=1/0;
		//}catch(Throwable t) {
			//collector.addError(t);
		//}
	    //collector.checkThat("a",object.containsString("two"));
	    //fail("here");
	}
	
}
